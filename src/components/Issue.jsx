const Issue = (props) => {
    const { issue } = props;
    return (

        <li>
            <a href="https://api.github.com/repos/facebook/create-react-app/issues/10625"></a>
            
        </li>
    );
};

export default Issue;